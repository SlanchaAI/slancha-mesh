"""Vendored classifier assets: mmBERT-small ONNX INT8 embedder + treelite heads.

Shipped in the wheel (importlib.resources) so auto-routing works offline /
air-gapped — no download-on-first-use. Same artifacts slancha-local vendors;
origin: slancha-api classifier v1. Embedder base model: mmBERT-small
(Apache 2.0, https://huggingface.co/jhu-clsp/mmBERT-small) — see NOTICE.

Integrity: SHA256SUMS in this directory pins every blob; verify with
`shasum -a 256 -c SHA256SUMS` from this directory after any sync.
"""
