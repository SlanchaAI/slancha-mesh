2026-08-02 — BLOCKER: JSON media routing concatenated a node-supplied URL path/query with a trusted protocol path, allowing node metadata to alter the upstream request target (Task 2 review, fixed before merge).
2026-08-02 — BLOCKER: `router --token` satisfied the non-loopback bind guard although listener authentication reads only `SLANCHA_NODE_TOKEN`, permitting an unauthenticated exposed router (final security review, fixed before merge).
2026-08-02 — BLOCKER: async video requests forwarded client-controlled remote-reference and runtime model-path fields to vLLM-Omni, exposing upstream SSRF and unsafe pickle-loading paths (final security review, fixed before merge).
2026-08-02 — BLOCKER: public branch commits and tracked release artifacts contained private fleet identifiers and personal filesystem paths (final security review; HEAD sanitized, clean-history replacement required before publication).
2026-08-02 — BLOCKER: the README tailnet quickstart left node-info on loopback and placed discovery after a foreground server, so a gateway could not discover the advertised specialist (OSS evidence review, fixed before replacement PR).
2026-08-02 — BLOCKER: the README bound Ollama to one private interface but Mesh still probed its default bind address, so the documented macOS/tailnet activation could not reach the backend (OSS evidence review, fixed before replacement PR).
