# Build context is the repo root; keep the registry image lean.
.git
.github
.venv
venv
__pycache__
*.pyc
.pytest_cache
.ruff_cache
.gitnexus
.claude
corpus
docs
deploy
tests
mesh/tests
# uv.lock is NOT ignored (#144) — the build stage needs it to `uv export
# --frozen` a pinned dependency closure instead of resolving fresh from PyPI.
*.md
!README.md
